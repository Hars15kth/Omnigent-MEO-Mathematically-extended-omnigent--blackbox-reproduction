# Release Assets

Attach these files alongside the GitHub handoff zip:

- `ARCHIVED_EVIDENCE_VERIFICATION_KIT.zip`
- `INDEPENDENT_OMNIGENT_REPLICATION_CONTRACT_24ARM.zip`

They are intentionally supplied as separate release assets rather than nested inside the handoff zip. This keeps extraction simple and avoids nested-archive/path-length issues on Windows.
